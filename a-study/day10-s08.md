# 第10天：s08 Context Compact 阈值实验

今天只解决一个核心问题：

> 当上下文真正超过阈值时，Harness如何把完整历史变成“当前请求 + 状态摘要”，并继续保留可追溯的原始记录？

建议投入 **75～90分钟**。

第9天已经理解并离线验证前三层：大结果落盘、中间历史归档、旧结果占位。今天专门验证第四层`compact_history`和补救层`reactive_compact`。

## 今日定位

- 所属层：Harness上下文管理层
- 解决问题：确定性裁剪之后，历史仍然超过上下文预算
- 接入位置：每次模型调用前，以及API返回上下文过长错误后
- 重要程度：核心
- 今日深度：看见摘要替换前后的结构变化，不研究摘要Prompt优化

总流程：

```text
prepare(messages)
→ 前三层确定性整理
→ estimate_chars仍超限
→ 保存完整transcript
→ 调用摘要器
→ history替换为一条压缩消息
→ 继续原任务
```

## 今日可见成果

今天结束时必须看到：

1. 降低阈值后自动触发`compact_history`
2. 压缩前后字符数明显下降
3. 原始历史完整写入transcript
4. 压缩消息同时包含当前用户请求和历史摘要
5. `reactive_compact`保留最近消息，只总结较早历史
6. 摘要失败或二次超长不会无限重试

## 第一步：先迁移到OpenAI Responses API

当前`s08`压缩器识别的是Anthropic消息结构：

```text
assistant(content=[tool_use])
user(content=[tool_result])
```

OpenAI Responses使用：

```text
function_call
function_call_output
```

因此迁移不能只替换SDK，还必须同步改写：

- 工具调用和结果识别
- `call_id`配对保护
- 新旧结果判断
- 批量结果预算
- transcript序列化

把下面这段直接交给Codex：

```text
请将s08_context_compact/code.py迁移为OpenAI Responses API版本，
参考已经迁移好的s06_subagent/code.py。

要求：
1. 使用OPENAI_API_KEY、OPENAI_MODEL_ID、OPENAI_BASE_URL和
   client.responses.create。
2. 工具定义改为type、name、description、parameters。
3. 使用function_call、function_call_output和call_id，arguments用
   json.loads解析；每轮保留全部response.output。
4. summarize_history也使用Responses API，并通过response.output_text
   获取摘要。
5. 将压缩器中所有tool_use/tool_result识别改为
   function_call/function_call_output，并保护相同call_id的一对调用与结果，
   不能在两者中间裁剪。
6. tool_result_budget和micro_compact改为处理function_call_output的
   output字段；未被下一次模型响应读取的新结果必须保持完整。
7. 保留四层压缩顺序、compact工具、active_request、transcript、
   reactive_compact和最多一次补救重试。
8. Hook改为接收tool_name、arguments和output，不依赖Anthropic block。
9. 所有面向人的注释、提示和错误信息使用中文。
10. 不新增依赖，不抽象通用Agent类，不改变压缩阈值。
11. 修改后运行py_compile，并用合成消息离线验证：大结果落盘、
    旧结果缩短、最新未读结果保留、call_id配对不被拆散。
```

迁移完成后再继续下面的阈值实验。第9天仍可不调用API，因为前三层是纯Python处理；第10天要调用真实模型验证第四层摘要和任务续接。

## 0～10分钟：复习第9天

闭卷回答：

```text
四层压缩顺序是什么？
前三层为什么不需要模型？
哪一层可能遗漏事实？
为什么未消费工具结果不能提前裁剪？
```

如果回答不出，只回看`day9-s08.md`中的总图，不重新通读整个README。

## 10～25分钟：追踪第四层代码

只阅读：

```python
estimate_chars()
summary_input()
summarize_history()
summary_message()
compact_history()
prepare()
```

重点理解三份信息：

```text
active_request
→ 当前用户仍然要求完成什么

summary
→ 历史中仍需保留的事实状态

transcript
→ 完整、可追溯但不再全部发送给模型的原文
```

压缩后不是“什么都忘了”，而是形成三个层次：

```text
模型当前看到：请求 + 摘要
磁盘仍然保存：完整transcript
已落盘工具输出：单独的结果文件
```

## 25～40分钟：降低阈值并准备真实实验

迁移和离线检查通过后，临时将：

```python
CONTEXT_CHAR_LIMIT = 50000
```

改为：

```python
CONTEXT_CHAR_LIMIT = 5000
```

只改这一处，实验完成后恢复为`50000`。

同时在`compact_history()`前后临时打印：

```text
压缩前消息数和字符数
压缩后消息数和字符数
```

你的所有权动作：运行前先预测压缩后的消息结构，不要让Codex解释完再看结果。

## 40～55分钟：观察自动摘要

运行`s08_context_compact/code.py`，输入：

```text
请依次读取s01_agent_loop、s02_tool_use和s03_permission的README.zh.md，
比较三章分别给Harness增加了什么能力，最后给出一张三行表格。
```

确认：

```text
压缩前：多条历史消息，字符数超过阈值
       ↓
[auto compact]
       ↓
完整历史写入transcript
       ↓
真实模型生成状态摘要
       ↓
压缩后：通常只剩一条summary_message
```

压缩消息应类似：

```text
[Compacted]

Current user request:
当前真正要完成的任务

Conversation summary (reference only):
历史状态摘要

Full transcript:
完整历史文件路径
```

检查`active_request`不是从最后一条`role=user`猜出来的，而是单独传入。

## 55～65分钟：理解补救压缩

阅读`reactive_compact()`，今天不必故意制造API错误。

它的触发场景：

```text
字符估算认为没超限
→ API仍返回prompt_too_long
→ 保存transcript
→ 总结较早历史
→ 最近5条消息原样保留
→ 重试一次
```

对比：

| 方法 | 何时发生 | 保留策略 |
|---|---|---|
| compact_history | 主动估算超限或模型调用compact | 用一条摘要替换整个历史 |
| reactive_compact | API明确拒绝上下文 | 摘要较早历史并保留最近消息 |

检查`MAX_REACTIVE_RETRIES = 1`：同类错误再次出现时应抛出，而不是无限压缩重试。

## 65～75分钟：理解主动compact工具

只追踪：

```python
COMPACT_TOOL
compact_requested
```

关键顺序：

```text
一次模型响应包含write_file和compact
→ 两个工具请求都先得到对应结果
→ 完整批次闭合
→ 再执行历史压缩
```

不能遇到`compact`就立刻清空历史，否则同一批次其他工具调用可能失去结果配对，文件副作用也可能被模型重复执行。

## 可选15分钟：量化压缩效果

如果今天有90分钟，在实验中增加：

```text
压缩率 = 1 - 压缩后字符数 / 压缩前字符数
```

输出：

```text
压缩前字符数：
压缩后字符数：
压缩率：
transcript文件大小：
```

不要追求某个固定压缩率，只验证信息从模型上下文转移到了摘要和磁盘。

## 数据流

```text
长messages
→ prepare前三层整理
→ 字符估算超限
→ write_transcript
→ summary_input限制摘要输入大小
→ 模型生成状态摘要
→ summary_message重新绑定active_request
→ 压缩后的messages继续进入Agent Loop
```

## 今日复盘

```text
自动摘要触发条件：
active_request为什么单独传入：
summary保存什么：
transcript保存什么：
compact_history与reactive_compact的区别：
为什么只补救重试一次：
为什么compact工具要等本批工具结果全部回填：
摘要层最大的风险：
```

## 验收标准

- 实际触发一次自动摘要
- 压缩后字符数明显下降
- 能从transcript找到完整原始历史
- 压缩消息保留当前请求、摘要和原文路径
- 能解释主动压缩与API错误补救的区别
- 摘要后Agent仍完成了原来的三章比较任务

## 今天不要做

- 不优化摘要Prompt
- 不研究精确Token计算
- 不接入模型供应商原生Compaction API
- 不删除transcript
- 不把摘要当成无损数据
- 不进入长期记忆实现
- 实验结束后不要忘记把阈值恢复为50000

今天最重要的认知：

> 自动摘要不是为了保存全部历史，而是把“继续完成当前任务所需的状态”留在模型上下文中，同时把完整原文移到可追溯的磁盘记录。
